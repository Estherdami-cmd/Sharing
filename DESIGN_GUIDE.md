# Sharing 디자인 가이드

> 토스(Toss)의 절제된 미니멀리즘을 기준으로 삼은 모노크롬 디자인 시스템.
> 색은 거의 쓰지 않고, **한 가지 라벤더(#d1bbf2)만 의미 있는 순간에** 쓴다.

---

## 1. Design System Overview

### 1.1 디자인 원칙

| 원칙 | 설명 | 이 서비스에서의 적용 |
|---|---|---|
| **한 화면 한 목적** | 한 화면에서 사용자가 내릴 결정은 하나 | 기부자 플로우를 4단계로 쪼갬. 각 단계에 CTA는 하나만 |
| **색은 정보다** | 장식용 색을 쓰지 않는다. 색이 있으면 뜻이 있다 | 라벤더 = 진행·성취, 빨강 = 차단, 나머지는 전부 회색 |
| **숫자를 크게** | 사용자가 확인할 값은 본문보다 확실히 크게 | 진행률 `%`, 채워진 개수, 남은 수량 |
| **다음 행동을 문장으로** | 버튼에 "확인" 대신 무슨 일이 일어나는지 | `여기에 나눔하기`, `수락 (진행률 +10)` |
| **실패를 미리 막는다** | 못 누르게 하고 이유를 말한다 | 유통기한 지난 식품은 다음 버튼 비활성 + 사유 표시 |

### 1.2 왜 모노크롬인가

이 서비스의 핵심 화면은 **진행률 바가 여러 개 나열된 목록**이다. 카드마다 다른 색을 쓰면 진행률 바의 색이 의미를 잃는다.
배경·텍스트·테두리를 전부 무채색으로 눌러두면, **라벤더 진행률 바 하나가 화면에서 유일하게 튀는 요소**가 된다.
사용자의 시선이 "얼마나 찼는가"로 바로 간다.

### 1.3 타이포그래피

Pretendard 우선, 없으면 Apple SD Gothic Neo. 한글 가독성 때문에 시스템 폰트보다 Pretendard를 앞에 둔다.

| 토큰 | 크기 / 행간 | 굵기 | 자간 | 용도 |
|---|---|---|---|---|
| `display` | 28px / 1.35 | 800 | -0.03em | 페이지 대표 제목 |
| `title` | 20px / 1.4 | 700 | -0.02em | 섹션 제목, 카드 품목명 |
| `body-lg` | 17px / 1.5 | 600 | -0.01em | 강조 본문 |
| `body` | 15px / 1.6 | 400 | -0.01em | 기본 본문 |
| `caption` | 13px / 1.5 | 400 | 0 | 보조 설명 |
| `label` | 12px / 1.4 | 700 | 0.01em | 폼 라벨, 뱃지 |
| `numeric` | 24px / 1.2 | 800 | -0.02em | 진행률 숫자 |

> **한글 자간 규칙**: 한글은 라틴보다 글자폭이 커서, 큰 글씨일수록 자간을 좁혀야 덩어리로 읽힌다.
> 24px 이상은 `-0.02em ~ -0.03em`, 본문은 `-0.01em`, 13px 이하는 조이지 않는다.

### 1.4 간격 · 모서리 · 그림자

**간격** — 4px 배수. 카드 내부는 12px, 카드 사이는 12px, 섹션 사이는 32px.

**모서리** — 토스 특유의 넉넉한 라운드를 따른다.

| 토큰 | 값 | 적용 |
|---|---|---|
| `rounded-lg` | 12px | 입력 필드, 작은 버튼 |
| `rounded-xl` | 16px | 카드, 기본 버튼 |
| `rounded-2xl` | 20px | 큰 카드, 모달 |
| `rounded-full` | 9999px | 뱃지, 칩, 탭, 진행률 바 |

**그림자** — 토스는 그림자를 거의 쓰지 않는다. 카드 구분은 **1px 테두리**로 한다.

| 토큰 | 값 | 적용 |
|---|---|---|
| `shadow-none` | 없음 | 기본 카드 (테두리로 구분) |
| `shadow-sm` | `0 1px 2px rgb(25 31 40 / 0.04)` | 떠 있는 느낌이 필요한 하단 고정 바 |
| `shadow-md` | `0 4px 16px rgb(25 31 40 / 0.08)` | 모달, 드롭다운 |

---

## 2. Color Palette (Tailwind)

### 2.1 Primary — Lavender

`#d1bbf2`를 500으로 두고 명도 스케일을 만들었다. 원본이 이미 밝은 톤이라 **500을 텍스트 색으로 쓰면 대비가 부족**하다(흰 배경 위 1.6:1). 그래서 역할을 나눈다.

| 단계 | HEX | 용도 |
|---|---|---|
| `primary-50` | `#faf7fe` | 선택된 카드 배경, 아주 옅은 강조 영역 |
| `primary-100` | `#f3edfc` | 뱃지 배경, 호버 배경 |
| `primary-200` | `#e8dcf9` | 진행률 바 트랙(강조 맥락), 구분선 |
| `primary-300` | `#dccbf6` | 비활성 상태의 강조 요소 |
| `primary-400` | `#d9c5f4` | 그라데이션 밝은 쪽 |
| **`primary-500`** | **`#d1bbf2`** | **진행률 바 채움, 선택 테두리 — 브랜드 기준색** |
| `primary-600` | `#b394e8` | 버튼 호버 |
| `primary-700` | `#9670d9` | **주요 버튼 배경, 뱃지 텍스트** (흰 글씨 대비 4.6:1 ✅) |
| `primary-800` | `#7852b8` | 버튼 액티브(눌림) |
| `primary-900` | `#5c3d8c` | 밝은 배경 위 강조 텍스트 (7.2:1 ✅) |
| `primary-950` | `#3a2659` | 최고 대비 텍스트 |

> **접근성 판단**: `primary-500`은 **면(面)으로만** 쓴다 — 진행률 바, 테두리, 배경.
> **글자와 아이콘에는 `primary-700` 이상**을 쓴다. 이 규칙 하나로 WCAG AA를 통과한다.

### 2.2 Neutral — Cool Gray

토스 UI의 회색 계열을 기준으로 잡았다. 순수 회색보다 **살짝 푸른 기**가 돌아야 라벤더와 같이 놓았을 때 색이 탁해 보이지 않는다.

| 단계 | HEX | 용도 |
|---|---|---|
| `neutral-50` | `#f9fafb` | 페이지 배경 |
| `neutral-100` | `#f2f4f6` | 진행률 바 트랙, 비활성 버튼 배경, 탭 그룹 배경 |
| `neutral-200` | `#e5e8eb` | **카드 테두리, 구분선** (가장 많이 쓰임) |
| `neutral-300` | `#d1d6db` | 입력 필드 테두리 |
| `neutral-400` | `#b0b8c1` | 플레이스홀더, 비활성 텍스트 |
| `neutral-500` | `#8b95a1` | 보조 설명 텍스트 |
| `neutral-600` | `#6b7684` | 본문 보조 |
| `neutral-700` | `#4e5968` | 라벨, 부제목 |
| `neutral-800` | `#333d4b` | 강한 본문 |
| `neutral-900` | `#191f28` | **기본 텍스트, 보조 버튼 배경** |
| `neutral-950` | `#0d1117` | 최고 대비 |

### 2.3 Secondary / Accent

모노크롬이므로 별도 색상 계열을 늘리지 않는다. 역할만 정의한다.

| 역할 | 매핑 | 근거 |
|---|---|---|
| **Secondary** | `neutral-900` 배경 + 흰 글씨 | 토스의 검정 버튼. 주요 액션이 아닌 "다음 단계로" 이동에 사용 |
| **Accent** | `primary-500` | 진행률·선택 상태 전용. 다른 곳에 쓰지 않는다 |

### 2.4 Semantic

상태 전달에만 쓴다. 채도를 낮춰 라벤더보다 튀지 않게 조정했다.

| 역할 | 배경 | 텍스트 | 사용처 |
|---|---|---|---|
| **success** | `#e8f7ee` | `#0f8a4c` | 목표 달성, 수락됨, 나눔 가능 |
| **warning** | `#fff6e5` | `#b26a00` | 기한 임박, 확인 대기중 |
| **danger** | `#fdeced` | `#d13c40` | 나눔 불가, 거절됨, 긴급 요청 |

> **긴급(urgent)에 danger 색을 쓰는 이유**: "긴급"은 부정적 상태가 아니라 **주목 요청**이다.
> 하지만 목록에서 즉시 눈에 띄어야 하므로, 라벤더(진행률)와 충돌하지 않는 빨강 계열을 배정했다.

### 2.5 Tailwind v4 설정

Tailwind v4는 JS 설정 파일 대신 CSS에서 `@theme`으로 토큰을 선언한다.

```css
/* app/globals.css */
@import "tailwindcss";

@theme {
  --color-primary-50:  #faf7fe;
  --color-primary-100: #f3edfc;
  --color-primary-200: #e8dcf9;
  --color-primary-300: #dccbf6;
  --color-primary-400: #d9c5f4;
  --color-primary-500: #d1bbf2;
  --color-primary-600: #b394e8;
  --color-primary-700: #9670d9;
  --color-primary-800: #7852b8;
  --color-primary-900: #5c3d8c;
  --color-primary-950: #3a2659;

  --color-neutral-50:  #f9fafb;
  --color-neutral-100: #f2f4f6;
  --color-neutral-200: #e5e8eb;
  --color-neutral-300: #d1d6db;
  --color-neutral-400: #b0b8c1;
  --color-neutral-500: #8b95a1;
  --color-neutral-600: #6b7684;
  --color-neutral-700: #4e5968;
  --color-neutral-800: #333d4b;
  --color-neutral-900: #191f28;
  --color-neutral-950: #0d1117;

  --color-success-bg: #e8f7ee;
  --color-success-fg: #0f8a4c;
  --color-warning-bg: #fff6e5;
  --color-warning-fg: #b26a00;
  --color-danger-bg:  #fdeced;
  --color-danger-fg:  #d13c40;

  --font-sans: "Pretendard", "Apple SD Gothic Neo", system-ui, sans-serif;
}
```

---

## 3. Page Implementations

### 3.0 Root Route `/`

**핵심 목적** — 세 가지 역할(둘러보기 / 기부하기 / 기관 관리)의 진입점.
첫 화면은 **진행률 게시판**이다. 로그인이나 가입 없이, 들어오자마자 "지금 뭐가 필요한지"가 보여야 한다.

**레이아웃 구조**

```
┌─────────────────────────────────┐
│  데모 안내 문구 (caption)        │  ← 시연용. 운영 시 제거
├─────────────────────────────────┤
│  ┌───────────────────────────┐  │
│  │ 게시판 │ 기부자 │ 기관   │  │  ← Segmented Control (탭)
│  └───────────────────────────┘  │
├─────────────────────────────────┤
│                                 │
│      선택된 탭의 콘텐츠          │
│                                 │
└─────────────────────────────────┘
```

**핵심 컴포넌트**

| 컴포넌트 | 설명 | 텍스트 |
|---|---|---|
| `DemoNotice` | 데모임을 알리는 안내 | "데모 화면입니다 · AI 인식은 시뮬레이션이고, 입력한 데이터는 서버에 저장되지 않아 일정 시간 뒤 초기 상태로 돌아갑니다" |
| `SegmentedControl` | 3분할 탭 | `진행률 게시판` / `기부자 모드` / `기관 관리` |
| `TabPanel` | 콘텐츠 영역 | 세 패널 모두 마운트 유지, `display`로만 전환 |

> **왜 세 탭을 모두 마운트해두는가**: 기부자 플로우 3단계에서 게시판을 확인하고 돌아왔을 때
> 입력하던 내용이 사라지면 안 된다. 언마운트 대신 `display: none`으로 숨기고,
> 탭을 열 때마다 `refreshKey`를 올려 데이터만 다시 불러온다.

---

### 3.1 진행률 게시판 (기본 탭)

**핵심 목적** — "지금 어디에 무엇이 얼마나 부족한지"를 스크롤 한 번으로 파악. 기부 동기를 만드는 화면.

**레이아웃 구조**

```
┌──────────────────────────────────────┐
│           지금 필요한 것들            │  display
│  기관이 올린 목표를 여럿이 나눠 채워요 │  body, neutral-500
│      전체 136 / 285개 · 요청 8건      │  body-lg, primary-700
│              새로고침                 │  caption, 텍스트 버튼
├──────────────────────────────────────┤
│ ┌──────────────────────────────────┐ │
│ │ 포항 사랑의 열매        [긴급]   │ │  label / danger 뱃지
│ │ 성인용 기저귀 대형               │ │  title
│ │ 위생용품 · 포항시 남구 효자동     │ │  caption, neutral-500
│ │                                  │ │
│ │ 15 / 50개 채워짐          30%    │ │  numeric + primary-700
│ │ ███████░░░░░░░░░░░░░░░░░░░░░░    │ │  진행률 바 (primary-500)
│ │                                  │ │
│ │ 35개만 더 모으면 목표를 채워요    │ │  body-lg
│ │ 요양 어르신 12분께 매주 전달돼요  │ │  caption, neutral-500
│ └──────────────────────────────────┘ │
│              (카드 반복)              │
└──────────────────────────────────────┘
```

**핵심 컴포넌트**

| 컴포넌트 | 구성 | 비고 |
|---|---|---|
| `PageHeader` | 제목 + 설명 + 전체 집계 + 새로고침 | 집계는 `primary-700`으로 유일하게 색을 넣음 |
| `NeedCard` | 기관명 / 품목명 / 분류·주소 / 진행률 / 남은 수량 / 안내 | 긴급이면 테두리 `danger-fg/30` |
| `NeedProgress` | 숫자 + % + 바 + 대기중 표시 | 아래 3.5에서 상세 |
| `EmptyState` | 일러스트 + "아직 등록된 요청이 없어요" | 이미지: `https://picsum.photos/seed/empty-board/240/180` |

**정렬 규칙** — 긴급 먼저, 그다음 진행률 낮은 순.
가장 도움이 필요한 것이 위로 온다. 거의 다 찬 요청을 위에 두면 기부 동기가 약해진다.

**그리드**

| 브레이크포인트 | 컬럼 | 카드 최대폭 |
|---|---|---|
| mobile (320~767) | 1 | 100% |
| tablet (768~1023) | 2 | `grid-cols-2 gap-3` |
| desktop (1024~1439) | 2 | 컨테이너 `max-w-3xl` 중앙 정렬 |
| wide (1440~) | 3 | 컨테이너 `max-w-5xl`, `grid-cols-3` |

---

### 3.2 기부자 모드 — 4단계 플로우

상단에 항상 **StepIndicator**(4개 칩)가 있어 현재 위치를 알 수 있다.

| 단계 | 칩 텍스트 | 핵심 목적 |
|---|---|---|
| 1 | `1. 물품 등록` | 사진에서 품목·유통기한을 읽고 **나눔 가능 여부 판정** |
| 2 | `2. 매칭 확인` | 인식 결과 수정 + 가장 필요한 곳 선택 |
| 3 | `3. 기부 신청` | 수량·장소·날짜 확정 |
| 4 | `4. 신청 완료` | 상태 확인 + 영수증 요청 |

#### 단계 1 — 물품 등록

**핵심 목적**: 사진 한 장으로 등록 절차를 없앤다. 그리고 **나눔하면 안 되는 물건을 여기서 걸러낸다.**

```
┌──────────────────────────────────────┐
│  [1.물품등록] 2.매칭 3.신청 4.완료   │  StepIndicator
│                                      │
│      나눔할 물품을 등록해주세요       │  display
│   사진 한 장이면 품목과 유통기한을    │  body, neutral-500
│           읽어드려요                  │
├──────────────────────────────────────┤
│ ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐ │  점선 테두리
│ │        [ 사진 미리보기 ]         │ │  aspect-4/3, rounded-xl
│ │                                  │ │
│ │  [갤러리에서 선택] [촬영하기]     │ │  보조 버튼 2분할
│ │  [    AI로 확인하기          ]   │ │  주요 버튼 (primary-700)
│ │  데모 모드 [ 자동 인식     ▾ ]   │ │  Select
│ └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘ │
├──────────────────────────────────────┤
│ ┌──────────────────────────────────┐ │  인식 후에만 등장
│ │ AI 인식 결과          [나눔 불가]│ │  title + 상태 뱃지
│ │ 품목명   [ 흰 우유 1L        ]   │ │
│ │ 카테고리 [ 기타            ▾ ]   │ │
│ │ 유통기한 [ 2026-08-15        ]   │ │
│ │ ☐ 유통기한이 없는 품목이에요      │ │
│ │ ⚠ 유통기한이 3일 지났어요.        │ │  danger-fg
│ │    안전상 나눔이 어려워요          │ │
│ │ AI 신뢰도 87%                    │ │  caption
│ │ [   다른 물품 등록하기       ]   │ │  나눔 불가일 때만
│ └──────────────────────────────────┘ │
│ [     다음: 매칭 결과 확인       ]   │  비활성 시 neutral-100
└──────────────────────────────────────┘
```

| 컴포넌트 | 상태 | 시각 처리 |
|---|---|---|
| `UploadZone` | 비어 있음 | 점선 `primary-300`, 배경 `primary-50`, "아직 등록된 사진이 없어요" |
| | 사진 있음 | 실제 이미지, `object-cover` |
| `RecognitionCard` | ok | 뱃지 `success`, 사유 `success-fg` |
| | caution | 뱃지 `warning`, 사유 `warning-fg` |
| | blocked | 뱃지 `danger`, 사유 `danger-fg`, 다음 버튼 잠김 |
| `PrimaryCTA` | 활성 | `bg-neutral-900 text-white` |
| | 비활성 | `bg-neutral-100 text-neutral-400 cursor-not-allowed` |

> **핵심 인터랙션**: 유통기한 입력값을 바꾸면 **서버 왕복 없이 즉시** 뱃지와 버튼 상태가 바뀐다.
> 같은 판정 함수(`evaluateShareable`)를 클라이언트에서도 호출하기 때문이다.
> 발표 시 "기한을 고치면 바로 초록으로 바뀝니다"를 보여줄 수 있는 지점.

#### 단계 2 — 매칭 확인

**핵심 목적**: AI가 틀렸을 수 있으니 고칠 기회를 주고, **왜 이 순서인지 납득시킨 뒤** 선택하게 한다.

```
┌──────────────────────────────────────┐
│         어디에 나눔할까요?            │  display
│  이 물건이 가장 필요한 곳 순으로      │  body, neutral-500
│           정렬했어요                  │
├──────────────────────────────────────┤
│ ┌──────────────────────────────────┐ │  확정 정보 카드
│ │ 품목명 (다르면 수정하세요)        │ │
│ │ [ 성인용 기저귀 대형          ]  │ │
│ │ 카테고리 [ 위생용품         ▾ ]  │ │
│ │ 내 지역  [ 북구 죽도동      ▾ ]  │ │
│ └──────────────────────────────────┘ │
├──────────────────────────────────────┤
│ ┌──────────────────────────────────┐ │  1순위: primary-500 테두리 2px
│ │ [1순위] [긴급]                   │ │
│ │ 성인용 기저귀 대형               │ │  title
│ │ 포항 사랑의 열매 · 4.6km          │ │  caption
│ │ 운영일: 월, 화, 수, 목, 금        │ │  caption
│ │ 15 / 50개 채워짐          30%    │ │
│ │ ████████░░░░░░░░░░░░░░░░░░░░     │ │
│ │ 필요도                75 · 매우 필요│ │  label
│ │ 성인용 기저귀 대형이 35개 더      │ │  body
│ │ 필요해요 (긴급)                   │ │
│ │ [     여기에 나눔하기         ]  │ │
│ └──────────────────────────────────┘ │
└──────────────────────────────────────┘
```

**필요도 라벨 색상 매핑**

| 라벨 | 점수 | 텍스트 색 | 근거 |
|---|---|---|---|
| 매우 필요 | 70~100 | `primary-700` | 브랜드색으로 "여기가 답" 신호 |
| 필요 | 40~69 | `neutral-700` | 중립 |
| 여유 | 0~39 | `neutral-400` | 흐리게 — 선택은 가능하되 권하지 않음 |

**카테고리 불일치 안내** — 정확히 맞는 요청이 없으면 목록 위에 배너를 띄운다.
배경 `warning-bg`, 테두리 `warning-fg/20`, 텍스트 `warning-fg`.
문구: "위생용품를 정확히 요청한 기관이 없어요. 대신 지금 다른 물품을 기다리는 곳을 보여드릴게요"

#### 단계 3 — 기부 신청

**핵심 목적**: 날짜 조율의 왕복을 없앤다. **양쪽 다 되는 날만** 보여준다.

```
┌──────────────────────────────────────┐
│              나눔 신청                │
│  포항 사랑의 열매의 성인용 기저귀      │
│         대형 목표를 채워요            │
├──────────────────────────────────────┤
│ │ 15 / 50개 채워짐          30%    │ │  현재 진행률 재확인
│ │ ████████░░░░░░░░░░░░░░░░░░░░     │ │
│ │                                  │ │
│ │ 수량      [ 10              ]    │ │
│ │ 10개를 기부하면 진행률이 50%가    │ │  ★ 기여 미리보기
│ │ 돼요 · 남은 목표 35개             │ │
│ │                                  │ │
│ │ 전달 장소 [ 기관에 직접 전달  ▾ ] │ │
│ │ 내가 가능한 요일                  │ │
│ │ (일)(월)(화)(수)(목)(금)(토)      │ │  Chip 다중 선택
│ │ 내가 가능한 시간대 [ 상관없음 ▾ ] │ │
│ │ [    AI 추천 날짜 받기        ]  │ │
│ │ ─────────────────────────────    │ │
│ │ 양쪽 모두 가능한 날짜 3개 찾았어요 │ │  success-fg
│ │ ┌──────────────────────────────┐ │ │
│ │ │ 8월 19일 (수) · 오전 9-12시  │ │ │  선택 시 primary-50 배경
│ │ │ 기관 운영일(수) · 회원님      │ │ │  + primary-500 테두리
│ │ │ 가능 요일 · 오전 9-12시       │ │ │
│ │ └──────────────────────────────┘ │ │
│ │ 연락처 [ 010-0000-0000       ]   │ │
│ │ [     나눔 신청하기           ]  │ │
└──────────────────────────────────────┘
```

> **기여 미리보기의 근거**: 크라우드펀딩의 핵심 동기는 "내가 얼마나 기여하는가"다.
> 수량 입력값이 바뀔 때마다 `30% → 50%`를 실시간으로 계산해 보여주면,
> 수량을 늘리는 행동에 즉각적인 보상이 생긴다.

**교집합이 없을 때** — `warning-fg` 텍스트로 이유까지 말한다.
"두 분 모두 가능한 날이 없어요. 포항 생활지원센터는 화·목요일 오후 1-5시에만 받아요"

#### 단계 4 — 신청 완료

```
┌──────────────────────────────────────┐
│              신청 완료                │
│      기관의 확인을 기다리고 있어요     │
├──────────────────────────────────────┤
│ │ ● 기관 확인 대기중      상태 새로고침│  warning-fg / 텍스트 버튼
│ │ 나눔 품목                         │  label
│ │ 성인용 기저귀 대형 (위생용품)      │
│ │ 기관 · 채우는 목표                 │
│ │ 포항 사랑의 열매 · 성인용 기저귀   │
│ │ 15 / 50개 채워짐          30%    │
│ │ ████████░░▒▒▒▒░░░░░░░░░░░░░       │  ▒ = 대기중(빗금)
│ │ 기관 확인 대기중 10개 포함 시 50%  │  caption
│ │ 수량 / 전달                       │
│ │ 10개 · 8월 19일 오전 9-12시        │
│ │ 기관에 직접 전달                   │
│ │ 기부금 신청서 작성 여부            │
│ │ 미작성                            │  neutral-400 → 작성 시 success-fg
│ │ [   기부금 영수증 요청        ]   │  수락 상태에서만 노출
│ [     처음부터 다시 신청하기      ]  │
└──────────────────────────────────────┘
```

---

### 3.3 기관 관리 탭

**핵심 목적** — 기관 담당자의 두 가지 일: **필요한 걸 올리기**, **들어온 걸 처리하기**.

```
┌──────────────────────────────────────┐
│              기관 관리                │
│  필요한 물품을 올리고, 들어온 신청을   │
│            확인해요                   │
├──────────────────────────────────────┤
│ ┌──────────────────────────────────┐ │  primary-500 테두리 2px
│ │ 필요 물품 올리기                  │ │  title
│ │ 목표 수량을 정하면 기부자들이      │ │  caption
│ │ 나눠서 채워요                     │ │
│ │ 우리 기관   [ 포항 나눔 푸드뱅크▾]│ │
│ │ 필요한 품목 [ 성인용 기저귀 대형 ]│ │
│ │ 카테고리    [ 위생용품        ▾ ]│ │
│ │ 목표 수량   [ 50              ]  │ │
│ │ 안내 문구   [ (선택)           ]  │ │
│ │ ☐ 긴급으로 표시 (추천 상단에 노출)│ │
│ │ [     필요 물품 등록          ]  │ │
│ └──────────────────────────────────┘ │
├──────────────────────────────────────┤
│  등록한 요청 · 진행률                 │  title
│  (NeedCard 반복, 진행률 표시)         │
├──────────────────────────────────────┤
│  들어온 신청                          │  title
│ ┌──────────────────────────────────┐ │
│ │ 포항 사랑의 열매        [대기중]  │ │
│ │ 성인용 기저귀 대형 목표 채우기     │ │  primary-700
│ │ (현재 30%)                        │ │
│ │ 성인용 기저귀 대형 (위생용품)·10개 │ │
│ │ 희망일: 2026-08-19 오전 9-12시    │ │
│ │ 전달 장소: 기관에 직접 전달        │ │
│ │ 연락처: 010-1234-5678             │ │
│ │ [수락 (진행률 +10)] [   거절   ]  │ │
│ └──────────────────────────────────┘ │
└──────────────────────────────────────┘
```

> **`수락 (진행률 +10)`이라는 버튼 문구**: 토스식 "결과를 미리 말해주는 버튼".
> 담당자가 누르기 전에 무슨 일이 일어나는지 알 수 있어 실수를 줄인다.

**그리드** — desktop 이상에서 좌우 2단.

| 브레이크포인트 | 레이아웃 |
|---|---|
| mobile / tablet | 1단 세로 (등록 폼 → 진행률 → 신청) |
| desktop 이상 | `lg:grid-cols-2` — 좌: 등록 폼 + 진행률 / 우: 들어온 신청 |

---

### 3.4 `/foodbank` — 기관 전용 딥링크

**핵심 목적** — 시연 시 **두 번째 창**으로 띄워 기관 역할을 맡기기 위한 라우트.
탭 없이 기관 관리 화면만 전체 폭으로 보여준다. 컴포넌트는 3.3과 동일하게 재사용한다.

---

### 3.5 NeedProgress — 공용 진행률 컴포넌트

이 서비스에서 **가장 많이 반복되는 요소**라 별도로 정의한다.

```
15 / 50개 채워짐                    30%
████████░░▒▒▒▒░░░░░░░░░░░░░░░░░░░░░░
기관 확인 대기중 10개 포함 시 50%
```

| 구성 | 스타일 | 비고 |
|---|---|---|
| 채워진 수 | `numeric` (24px/800) `neutral-900` | 시선이 가장 먼저 닿는 값 |
| `/ 50개 채워짐` | `caption` `neutral-500` | |
| 퍼센트 | `body-lg` (17px/800) 진행률별 색 | 우측 정렬 |
| 트랙 | `h-2.5 bg-neutral-100 rounded-full` | |
| 채움 | `bg-primary-500 rounded-full` + `transition-[width] duration-500` | |
| 대기중 | 45° 빗금 (`repeating-linear-gradient`, 같은 색 33% 투명도) | 확정 아님을 시각적으로 구분 |

**진행률 구간별 색**

| 구간 | 색 | 근거 |
|---|---|---|
| 100% | `success-fg` `#0f8a4c` | 달성 — 축하의 의미 |
| 60~99% | `primary-600` `#b394e8` | 순항 |
| 30~59% | `primary-500` `#d1bbf2` | 브랜드 기준색 |
| 0~29% | `danger-fg` `#d13c40` | **도움이 시급함을 알림** |

> **낮은 진행률에 빨강을 쓰는 이유**: 0%가 회색이면 "아무도 관심 없는 요청"으로 보인다.
> 빨강이면 "여기가 제일 급하다"로 읽힌다. 같은 데이터라도 기부 행동을 유도하는 방향으로 색을 배정했다.

---

## 4. Layout Components

### 4.1 AppShell

| 항목 | 내용 |
|---|---|
| **적용 라우트** | `/`, `/foodbank` |
| **역할** | 페이지 배경, 최대 폭, 좌우 여백, 세로 리듬 |
| **구조** | `min-h-screen bg-neutral-50 flex flex-col items-center px-5 pt-8 pb-12` |

**반응형**

| BP | 좌우 패딩 | 콘텐츠 최대폭 |
|---|---|---|
| mobile | `px-5` (20px) | 100% |
| tablet | `md:px-6` | `md:max-w-2xl` (672px) |
| desktop | `lg:px-8` | `lg:max-w-3xl` (768px) |
| wide | `xl:px-10` | `xl:max-w-5xl` (1024px) |

### 4.2 SegmentedControl (탭)

| 항목 | 내용 |
|---|---|
| **적용 라우트** | `/` 만 |
| **구조** | `bg-neutral-100 rounded-full p-1 flex gap-1` |
| **선택됨** | `bg-neutral-900 text-white rounded-full` |
| **선택 안 됨** | `text-neutral-600 hover:text-neutral-900` |

**반응형** — mobile에서 3개 탭이 좁아지므로 `text-[13px] px-3`,
tablet 이상 `text-sm px-5`. 절대 줄바꿈되지 않게 `whitespace-nowrap`.

### 4.3 Card

| 항목 | 내용 |
|---|---|
| **적용** | 모든 화면 |
| **기본** | `bg-white border border-neutral-200 rounded-2xl p-5 flex flex-col gap-3` |
| **강조(1순위)** | `border-2 border-primary-500` |
| **긴급** | `border-2 border-danger-fg/30` |
| **선택됨** | `border-2 border-primary-500 bg-primary-50` |

### 4.4 Form Controls

| 컴포넌트 | 기본 | 포커스 |
|---|---|---|
| `Input` / `Select` | `w-full h-12 px-4 rounded-xl border border-neutral-300 text-[15px]` | `border-primary-500 ring-4 ring-primary-100 outline-none` |
| `Label` | `text-xs font-bold text-neutral-700` | — |
| `Chip` (요일) | `flex-1 h-10 rounded-full border border-neutral-300 text-neutral-600` | 선택 시 `bg-primary-500 border-primary-500 text-neutral-900` |

> **칩 선택 시 글자색이 흰색이 아닌 `neutral-900`인 이유**: `primary-500`이 밝은 라벤더라
> 흰 글씨는 대비 1.5:1로 읽히지 않는다. 어두운 글씨를 얹으면 8.4:1로 충분하다.

### 4.5 Button

| 종류 | 스타일 | 용도 |
|---|---|---|
| **Primary** | `bg-primary-700 text-white h-14 rounded-xl font-bold` | 화면의 핵심 행동 (AI로 확인하기, 나눔 신청하기) |
| **Secondary** | `bg-neutral-900 text-white h-14 rounded-xl font-bold` | 단계 이동 (다음: 매칭 결과 확인) |
| **Outline** | `bg-white border-2 border-neutral-300 text-neutral-700` | 보조 (갤러리에서 선택, 거절) |
| **Ghost** | `text-neutral-400 text-[13px]` | 되돌아가기, 새로고침 |
| **Disabled** | `bg-neutral-100 text-neutral-400 cursor-not-allowed` | 조건 미충족 |

**터치 타깃** — 모든 버튼 최소 높이 44px. mobile에서 주요 버튼은 `h-14`(56px).

### 4.6 Badge

| 종류 | 스타일 | 텍스트 |
|---|---|---|
| 상태 ok | `bg-success-bg text-success-fg` | `나눔 가능`, `수락됨`, `목표 달성` |
| 상태 caution | `bg-warning-bg text-warning-fg` | `나눔 주의`, `대기중` |
| 상태 blocked | `bg-danger-bg text-danger-fg` | `나눔 불가`, `거절됨`, `긴급` |
| 순위 | `bg-primary-100 text-primary-700` | `1순위` |

공통: `text-xs font-bold px-2.5 py-1 rounded-full whitespace-nowrap`

---

## 5. Interaction Patterns

### 5.1 상태 전이

| 패턴 | 동작 | 지속시간 |
|---|---|---|
| **진행률 바 채움** | `width` 전환 | 500ms `ease-out` |
| **탭 전환** | 즉시 (애니메이션 없음) | 0ms |
| **카드 선택** | 테두리·배경 전환 | 150ms `ease-out` |
| **버튼 누름** | `active:scale-[0.98]` | 100ms |
| **뱃지 색 변경** | `transition-colors` | 200ms |

> **탭 전환에 애니메이션을 넣지 않는 이유**: 토스는 정보 밀도가 높은 목록 전환에 모션을 넣지 않는다.
> 매 전환마다 200ms를 기다리게 하면 반복 사용 시 느리게 느껴진다.

### 5.2 로딩

| 상황 | 처리 | 문구 |
|---|---|---|
| AI 인식 중 | 버튼 비활성 + 문구 교체 | "사진에서 품목과 유통기한을 읽는 중..." |
| 매칭 조회 | 텍스트 | "불러오는 중..." |
| 날짜 추천 | 버튼 문구 교체 | "가능한 날짜를 찾는 중..." |
| 신청 전송 | 버튼 문구 교체 | "신청 중..." |

> 스피너 대신 **버튼 문구를 바꾼다.** 무엇을 기다리는지 알려주는 편이 정보량이 많고, 레이아웃도 흔들리지 않는다.

### 5.3 실시간 피드백 (이 서비스의 핵심)

| 트리거 | 즉시 반응 | 서버 왕복 |
|---|---|---|
| 유통기한 수정 | 뱃지 색·문구·다음 버튼 활성화 | 없음 (클라이언트 판정) |
| 수량 입력 | "진행률이 50%가 돼요" | 없음 |
| 지역 변경 | 거리·순위 재정렬 | 있음 (PATCH → 재조회) |
| 카테고리 변경 | 추천 목록 교체 | 있음 |

### 5.4 오류 처리

인라인 표시만 쓴다. 토스트나 얼럿을 쓰지 않는다 — **문제가 생긴 자리에서 말한다.**

| 위치 | 스타일 | 예시 |
|---|---|---|
| 폼 하단 | `text-[13px] text-danger-fg` | "수량·날짜·장소·연락처를 모두 입력해주세요" |
| 판정 사유 | `text-[13px] font-bold` + tone 색 | "유통기한이 3일 지났어요. 안전상 나눔이 어려워요" |
| 안내 배너 | `bg-warning-bg border border-warning-fg/20 rounded-xl p-3` | 카테고리 불일치 안내 |

### 5.5 빈 상태

| 화면 | 이미지 | 문구 |
|---|---|---|
| 게시판 | `https://picsum.photos/seed/empty-board/240/180` | "아직 등록된 요청이 없어요" |
| 신청 목록 | `https://picsum.photos/seed/empty-apply/240/180` | "아직 들어온 신청이 없어요" |
| 업로드 전 | 이미지 없음 (점선 박스) | "아직 등록된 사진이 없어요" |

### 5.6 접근성

| 항목 | 규칙 |
|---|---|
| 색 대비 | 본문 4.5:1 이상. `primary-500`은 면에만, 글자는 `primary-700` 이상 |
| 색 단독 의존 금지 | 상태를 색으로만 표현하지 않는다 — 뱃지에 항상 텍스트(`나눔 불가`) 병기 |
| 포커스 링 | `ring-4 ring-primary-100` — 키보드 탐색 시 명확히 보임 |
| 터치 타깃 | 최소 44×44px |
| 이미지 대체 텍스트 | 모든 `img`에 `alt` — 예: "업로드한 물품 미리보기" |

---

## 6. Breakpoints

```scss
$breakpoints: (
  'mobile':  320px,
  'tablet':  768px,
  'desktop': 1024px,
  'wide':    1440px
);
```

Tailwind 기본 접두사와 매핑한다.

| 이름 | 최소폭 | Tailwind | 기준 기기 |
|---|---|---|---|
| mobile | 320px | (기본) | iPhone SE ~ Pro Max |
| tablet | 768px | `md:` | iPad 세로 |
| desktop | 1024px | `lg:` | iPad 가로, 노트북 |
| wide | 1440px | `xl:` | 데스크톱 모니터 |

### 6.1 브레이크포인트별 전체 동작

| 요소 | mobile | tablet | desktop | wide |
|---|---|---|---|---|
| 컨테이너 최대폭 | 100% | 672px | 768px | 1024px |
| 좌우 패딩 | 20px | 24px | 32px | 40px |
| 게시판 카드 | 1열 | 2열 | 2열 | 3열 |
| 기관 관리 | 1단 | 1단 | 2단 | 2단 |
| 탭 글자 | 13px | 14px | 14px | 14px |
| 페이지 제목 | 24px | 28px | 28px | 32px |
| 주요 버튼 높이 | 56px | 52px | 52px | 52px |
| 업로드 영역 | 4:3 | 16:9 | 16:9 | 16:9 |

### 6.2 모바일 우선 원칙

이 서비스는 **갤러리에서 사진을 고르는 것**이 시작점이다. 사용자는 거의 확실히 휴대폰에 있다.
따라서 모든 스타일은 mobile 기준으로 먼저 쓰고, `md:` 이상에서 넓히기만 한다.

**모바일에서 특별히 지키는 것**

- 주요 버튼은 `h-14`(56px) — 한 손 조작 시 오조작 방지
- 요일 칩 7개는 `flex-1`로 균등 분할, 최소 40px 높이 확보
- 가로 스크롤 절대 금지 — 모든 컨테이너 `w-full max-w-full`
- 카메라 입력에 `capture="environment"` — 바로 후면 카메라가 열림

---

## 7. 구현 체크리스트

- [ ] `@theme`에 팔레트 토큰 선언
- [ ] `primary-500`이 텍스트로 쓰인 곳이 없는지 확인
- [ ] 모든 브레이크포인트에서 가로 스크롤 0
- [ ] 진행률 바에 `transition-[width]` 적용
- [ ] 버튼 최소 높이 44px
- [ ] 뱃지에 색과 텍스트가 모두 있는지
- [ ] 빈 상태 3종 구현
- [ ] `alt` 속성 누락 없음
